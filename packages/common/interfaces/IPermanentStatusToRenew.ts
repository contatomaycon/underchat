export interface IPermanentStatusToRenew {
  worker_profile_status_id: string;
  worker_id: string;
  account_id: string;
  worker_profile_status_type_id: string;
  value: string;
  is_permanent: boolean;
}
