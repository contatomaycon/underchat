export enum EReleasePermissions {
  release_group = 'release_group',
  release_view = 'release_view',
  release_create = 'release_create',
  release_update = 'release_update',
  release_delete = 'release_delete',
}
