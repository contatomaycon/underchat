export enum EScheduleStatus {
  pending = 'pending',
  processing = 'processing',
  processed = 'processed',
  sent = 'sent',
  failed = 'failed',
  limit_exhausted = 'limit_exhausted',
  ignored = 'ignored',
}
