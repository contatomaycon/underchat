export enum EScheduleStatus {
  pending = 'pending',
  processing = 'processing',
  paused = 'paused',
  canceled = 'canceled',
  processed = 'processed',
  sent = 'sent',
  failed = 'failed',
  limit_exhausted = 'limit_exhausted',
  ignored = 'ignored',
}
