export enum EWorkerWarmPoolState {
  warming = 'warming',
  ready = 'ready',
  reserved = 'reserved',
  activating = 'activating',
  assigned = 'assigned',
  deleting = 'deleting',
  error = 'error',
}
