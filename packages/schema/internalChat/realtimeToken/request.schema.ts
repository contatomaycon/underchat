import { Type } from '@sinclair/typebox';

export const realtimeTokenParamsSchema = Type.Object({});
export const realtimeTokenQuerySchema = Type.Object({});
export const realtimeTokenBodySchema = Type.Object({});
